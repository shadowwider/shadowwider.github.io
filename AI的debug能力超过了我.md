# AI 的 Debug 能力，怎么就超越了我？



前几天我买了一个 API 中转站的 Coding Plan，准备借用 Cloudflare Worker 做一层请求分发，主要是为了方便后续自己做计费和路由。



这种转发逻辑我以前写过很多次，原本以为是个半小时就能搞定的轻巧活儿。结果代码放进去之后，接口死活通不了——只要发起请求，拿到的永远不是预期中的 JSON，而是一串直接打到对方官网的主页 HTML 源码。



我反复排查过，如果不走这层 Worker 转发，接口毫无问题；如果用同样的代码去转发别的域名，也完全正常。唯独拼到这个中转站的接口上，直接瘫痪。



当时我整个人是懵的，觉得这事简直匪夷所思。我的第一反应是：这绝对是对方公司做了某种针对 Cloudflare Worker 的反爬或禁用策略，压根不想让人二次转发。



**我们总是习惯把解释不了的异常，归咎于某种不可抗的外部规则。**



既然此路不通，我索性换了 LiteLLM 的方案，顺手给中转站的客服发了张工单，带着点怀疑问了一句：“你们是不是针对 Cloudflare 的转发做了特殊限制？”



接工单的客服看起来是个小伙子。他说帮我跑去研究研究。



到了下午，他居然真把处理结果发过来了，附带几张截屏，告诉我：“搞定了。”



我仔细看了下那几张图，才发现解决问题的根本不是他，而是他丢给 AI 的几轮对话日志。



AI 在刚接手时，遇到了和我一模一样的困境：请求发送，返回网页。但不同的是，我选择了绕路，而 AI 选择了**往死里研究**。



它沿着运行日志一步步追查，做了一连串深度分析，最后定位到了一个让我哭笑不得的盲区：原来是因为接口路径里带有 `/gateway` 这个词。Cloudflare Worker 在做内部路由解析时，似乎对这个特定的词做了某种特殊拦截，直接把它给“吞”掉了。这就导致真正的请求路径被截断，一路迷路打到了根域名的主页上。



更让人绝望的是后续。AI 不仅定位出了问题，它甚至不知道从哪里“吃透”了 Cloudflare Worker 内部极度冷门的匹配规则，创造性地调整了 URL 的拼接顺序——把域名和特定参数放前面，把那个被吞掉的 `/gateway` 路径用一种非常规的写法挂在最后，成功绕过了这个 Bug，顺利拿到了数据。



看到那个结论的瞬间，我笑了，随后心里升起一股复杂的敬畏感。



我从来没想过是路由解析本身在作祟。但我不得不承认：**AI 在这件事情上展现出的执着与创造力，真的远超了我。**



我开始反思，为什么在这场简单 Debug 里，我输给了 AI？



**第一，是对“边界”的执着差异。**

对我来说，没有抓包手段、看不到底层转发过程，就等于“巧妇难为无米之炊”。我顺理成章地放弃了。但 AI 没有“放弃”这个选项，它的目标极度纯粹——一定要把这件事情做通。



为了达成这个目标，它会像个不知疲倦的侦探一样做成百上千次实验：为什么会打到这个网页？这个网页到底是从哪来的？如果路径被吞了，把参数换个位置会怎样？



我们总以为创造力是灵光一闪，其实很多时候，**极致的创造力只是源于不达目的不罢休的穷尽实验。**



**第二，是对“上下文”的利用效率。**

程序员排查问题，唯一的抓手就是 Log。没有日志，所有推演都是无源之水。我当时觉得信息不足，其实是我忽略了已有的运行日志；而 AI 抓住了每一个输入的细微变化，把原本极其有限的上下文用到了极致。在信息完全对等的情况下，谁对上下文的挖掘更深，谁就能拿到解药。



**第三，也是最重要的一点——我的“选项”比它多。**

在真实世界里，我的选择太多了。我觉得对方可能做了屏蔽，我觉得可以换个代理工具，我觉得这事不值得再花两小时……当我心里有了“这可能本来就无解”或者“我有退路”的预期时，分配给“硬磕底线”的精力就被稀释了。



而 AI 的世界里没有退路，在它的计算逻辑里，这个问题必须被解决。



这像极了我们做产品或者创业的过程。很多时候，能陪我们走到最后的，往往不是最聪明的那条路径，而是把退路切断后的那一股傻劲。



说到底，有时候阻碍我们找到答案的，不是能力不够，而是选择太多。

